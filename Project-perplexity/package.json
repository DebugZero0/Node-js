{
  "name": "project-perplexity-root",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm install && npm run build && cd ../Backend && npm install",
    "start": "node Backend/server.js"
  }
}